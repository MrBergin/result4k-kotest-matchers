rootProject.name = "result4k-kotest-matchers"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        jcenter()
        mavenCentral()
    }
}

include(":samples")
